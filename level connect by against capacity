 SELECT LEVEL AS D, LEVEL AS R
FROM AB_SETUP_LOCKER_REGISTRATION
WHERE CAPACITY IS NOT NULL
  AND LOCKER_ID = :P219_BLOCK_ID
CONNECT BY LEVEL <= CAPACITY
AND PRIOR LOCKER_ID = LOCKER_ID
AND PRIOR DBMS_RANDOM.VALUE IS NOT NULL;
